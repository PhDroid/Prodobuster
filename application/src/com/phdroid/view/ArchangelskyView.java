package com.phdroid.view;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.view.View;

public class ArchangelskyView extends View {
	public ArchangelskyView(Context context) {
		super(context);
	}

	public ArchangelskyView(Context context, AttributeSet attrs) {
		super(context, attrs);
	}

	public ArchangelskyView(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
	}

	@Override
	protected void onDraw(Canvas canvas) {
		super.onDraw(canvas);
	}
}
